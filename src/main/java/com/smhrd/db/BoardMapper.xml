<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.db.MemberMapper">
    
    <!-- 게시판  -->
    
    <select id="select" resultType="com.smhrd.Model.MyBoard">
	select * from t_notice
	</select>
	
	<insert id="write" parameterType="com.smhrd.Model.MyBoard">
	insert into t_notice(not_idx, not_title, not_content, not_file, not_create_at) values(#{title}, #{content}, #{writer}, #{img})
	</insert>
	
	<select id="getBoard" parameterType="int" resultType="com.smhrd.model.MyBoard">
	select * from myboard where idx=#{idx}
	</select>
	
	<delete id="delete">
	delete from myboard where idx=#{idx}
	</delete>
	
	<select id="board_cnt" resultType="String">
	select count(*) as board_cnt from myboard
	</select>
</mapper>