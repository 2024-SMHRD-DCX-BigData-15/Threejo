<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="RequestMapper">

    <!-- 모든 의뢰 데이터 조회 -->
    <select id="selectAllRequests" resultType="com.smhrd.Model.OrderVO">
        SELECT 
            svc_idx AS svcIdx,
            svc_title AS svcTitle,
            svc_content AS svcContent,
            svc_file AS svcFile,
            user_id AS userId,
            created_at AS createdAt
        FROM requests;
    </select>
    
     <!-- 의뢰 상세 정보 조회 -->
    <select id="getOrderById" resultType="com.smhrd.OrderVO">
        SELECT writer, create_date AS createDate, status, description
        FROM orders
        WHERE order_id = #{orderId}
    </select>


</mapper>